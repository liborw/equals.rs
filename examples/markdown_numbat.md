# equals.rs Markdown (Numbat) demo

The following fenced block computes with physical units:

```numbat
let distance = 150 km
let time = 2 h
let speed = distance / time #=
speed * 30 min #=
```

Inline Numbat works too: `let g = 9.81 m / s^2; g * 2 s #=`.
